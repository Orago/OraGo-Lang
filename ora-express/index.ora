req express ~ 'express.npm';
imp herbert ~ './sayHiHerbert.ora';

@app = express();
@runs = 0;

* onSlash (req, res){
	@$ runs = runs + 1;
	
	> res.send ('Hello World ' + runs);
}

app.get('/', onSlash);

@port = 3000;

app.listen (port);

!"Listening on port" & port; 