require express FROM 'express.npm';
import herbert FROM './sayHiHerbert.ora';

let app = express();
let runs = 0;

function onSlash (req, res){
	let GLOBAL runs = runs + 1;
	
	return res.send ('Hello World ' + runs);
}

app.get('/', onSlash);

let port = 3000;

app.listen (port);


print "Listening on port" & port; 