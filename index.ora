FUNCTION createPerson (name, age) {
	FUNCTION getAge (test, d) {
		RETURN 5;
	};

	SET person         TO OBJECT;
	SET person.name    TO name;
	SET person.age     TO getAge();

	RETURN person;
}

PRINT "guh";
SET thomas TO createPerson("tom", 2);
LOG_VARIABLES;

DELETE thomas;

LOG_VARIABLES;
